# NOTE: there are alias suffixes configured for different backends:
#
# _all         all backends
# _unsafe    all backends, unsafe
#
# _no-std      all no-std backends
# _alloc       all alloc backends
#
# _ct          crossterm
# _nc          notcurses
# _sdl         sdl2
#
[alias]

# CHECK
cl = "clippy"
#
# backends:
cl_all = "clippy --features=backends_all"
cl_ct = "clippy --features=crossterm"
cl_nc = "clippy --features=notcurses"
cl_sdl = "clippy --features=sdl2"
# unsafe:
cl_unsafe = "clippy --no-default-features --features=backends_all,depura"
cl_no-std = "clippy --profile no-std --no-default-features --features=all_backends_no-std,depura"
cl_alloc = "clippy --profile no-std --no-default-features --features=all_backends_alloc,depura"

# CLEAN PACKAGE
cp = "clean --package"
crp = "clean --release --package"

# DOC
d = "doc --no-deps"
do = "doc --no-deps --open"
dd = "doc"
ddo = "doc --open"
#
d_all = "doc --no-deps --features=backends_all"
do_all = "doc --no-deps --open --features=backends_all"
dd_all = "doc --features=backends_all"
ddo_all = "doc --open --features=backends_all"
# +nightly; document everything
nd = "doc --no-deps --features=nightly,backends_all"
ndo = "doc --no-deps --open --features=nightly,backends_all"
ndd = "doc --features=nightly,backends_all"
nddo = "doc --open --features=nightly,backends_all"

# BUILD
b = "build"
bb = "build --bin"
be = "build --example"
br = "build --release"
brb = "build --release --bin"
bre = "build --release --example"
# crossterm
b_ct = "build --features=crossterm"
bb_ct = "build --features=crossterm --bin"
be_ct = "build --features=crossterm --example"
br_ct = "build --release --features=crossterm"
brb_ct = "build --release --features=crossterm --bin"
bre_ct = "build --release --features=crossterm --example"
# notcurses
b_nc = "build --features=notcurses"
bb_nc = "build --features=notcurses --bin"
be_nc = "build --features=notcurses --example"
br_nc = "build --release --features=notcurses"
brb_nc = "build --release --features=notcurses --bin"
bre_nc = "build --release --features=notcurses --example"
# sdl2
b_sdl = "build --features=sdl2"
bb_sdl = "build --features=sdl2 --bin"
be_sdl = "build --features=sdl2 --example"
br_sdl = "build --release --features=sdl2"
brb_sdl = "build --release --features=sdl2 --bin"
bre_sdl = "build --release --features=sdl2 --example"
# all
b_all = "build --features=backends_all"
bb_all = "build --features=backends_all --bin"
be_all = "build --features=backends_all --example"
br_all = "build --release --features=backends_all"
brb_all = "build --release --features=backends_all --bin"
bre_all = "build --release --features=backends_all --example"
# no-std | alloc
be_no-std = "build --profile no-std --no-default-features --features=backends_no-std --example"
be_alloc = "build --profile no-std --no-default-features --features=backends_alloc --example"
bre_no-std = "build --profile no-std-release --release --no-default-features --features=backends_no-std --example"
bre_alloc = "build --profile no-std-release --release --no-default-features --features=backends_alloc --example"


# RUN
r = "run"
rb = "run --bin"
re = "run --example"
rr = "run --release"
rrb = "run --release --bin"
rre = "run --release --example"
# crossterm
r_ct = "run --features=crossterm"
rr_ct = "run --release --features=crossterm"
rb_ct = "run --features=crossterm --bin"
re_ct = "run --features=crossterm --example"
rrb_ct = "run --release --features=crossterm --bin"
rre_ct = "run --release --features=crossterm --example"
# notcurses
r_nc = "run --features=notcurses"
rr_nc = "run --release --features=notcurses"
rb_nc = "run --features=notcurses --bin"
re_nc = "run --features=notcurses --example"
rrb_nc = "run --release --features=notcurses --bin"
rre_nc = "run --release --features=notcurses --example"
# sdl2
r_sdl = "run --features=sdl2"
rb_sdl = "run --features=sdl2 --bin"
re_sdl = "run --features=sdl2 --example"
rr_sdl = "run --release --features=sdl2"
rrb_sdl = "run --release --features=sdl2 --bin"
rre_sdl = "run --release --features=sdl2 --example"
# all
r_all = "run --features=backends_all"
rr_all = "run --release --features=backends_all"
rb_all = "run --features=backends_all --bin"
re_all = "run --features=backends_all --example"
rrb_all = "run --release --features=backends_all --bin"
rre_all = "run --release --features=backends_all --example"
# all_unsafe
r_all_unsafe = "run --no-default-features --features=backends_all,depura"
rr_all_unsafe = "run --release --no-default-features --features=backends_all,depura"
rb_all_unsafe = "run --no-default-features --features=backends_all,depura --bin"
re_all_unsafe = "run --no-default-features --features=backends_all,depura --example"
rrb_all_unsafe = "run --release --no-default-features --features=backends_all,depura --bin"
rre_all_unsafe = "run --release --no-default-features --features=backends_all,depura --example"
# no-std | alloc
re_no-std = "run --profile no-std --no-default-features --features=backends_no-std --example"
re_alloc = "run --profile no-std --no-default-features --features=backends_alloc --example"
rre_no-std = "run --profile no-std-release --release --no-default-features --features=backends_no-std --example"
rre_alloc = "run --profile no-std-release --release --no-default-features --features=backends_alloc --example"

# TEST
t = "test"
td = "test --doc"
t_ = "test -- --nocapture"
t_1 = "test -- --test-threads 1 --nocapture"
#
t_ct = "test --features=crossterm"
td_ct = "test --doc --features=crossterm"
#
t_nc = "test --features=notcurses"
td_nc = "test --doc --features=notcurses"
#
t_all = "test --features=backends_all"
td_all = "test --doc --features=backends_all"

# TREE
tr = "tree"
trf = "tree --format {p}:{f}" # with feature list
trr = "tree --format {p}:{r}" # with repository url
# crossterm
tr_ct = "tree --features=crossterm"
trf_ct = "tree --format {p}:{f} --features=crossterm"
trr_ct = "tree --format {p}:{r} --features=crossterm"
# notcurses
tr_nc = "tree --features=notcurses"
trf_nc = "tree --format {p}:{f} --features=notcurses"
trr_nc = "tree --format {p}:{r} --features=notcurses"
# sdl2
tr_sdl = "tree --features=sdl2"
trf_sdl = "tree --format {p}:{f} --features=sdl2"
trr_sdl = "tree --format {p}:{r} --features=sdl2"
# all
tr_all = "tree --features=backends_all"
trf_all = "tree --format {p}:{f} --features=backends_all"
trr_all = "tree --format {p}:{r} --features=backends_all"

# PUBLISH
p = "publish --dry-run"
pa = "publish --dry-run --allow-dirty"
pp = "publish"
ppa = "publish --allow-dirty"

# Cargo subcommands:
## cargo-expand
E = "expand"
Eb = "expand --bin"
Ee = "expand --example"
El = "expand --lib"
# crossterm
E_ct = "expand  --features=crossterm"
Eb_ct = "expand --bin --features=crossterm"
Ee_ct = "expand --example --features=crossterm"
El_ct = "expand --lib --features=crossterm"
# notcurses
E_nc = "expand  --features=notcurses"
Eb_nc = "expand --bin --features=notcurses"
Ee_nc = "expand --example --features=notcurses"
El_nc = "expand --lib --features=notcurses"
# sdl2
E_sdl = "expand  --features=sdl2"
Eb_sdl = "expand --bin --features=sdl2"
Ee_sdl = "expand --example --features=sdl2"
El_sdl = "expand --lib --features=sdl2"
# all
E_all = "expand  --features=backends_all"
Eb_all = "expand --bin --features=backends_all"
Ee_all = "expand --example --features=backends_all"
El_all = "expand --lib --features=backends_all"

[build]
